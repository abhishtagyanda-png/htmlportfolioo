// AnimatedStars is now a lightweight overlay — the global starfield
// is rendered by #star-canvas in App.jsx via pure CSS box-shadows.
// This component is kept as a transparent pass-through for the Hero section.
const AnimatedStars = () => null;
export default AnimatedStars;
